# Tsunami data plot utilities

Tsunami plot utilities.

## Building

## Build (Debug)

```sh
cd build
cmake --build . --config Debug
```

## Build (Release)

```sh
cd build
cmake --build . --config Release
```

## Clean

```sh
cd build
cmake --build . --target clean
```
